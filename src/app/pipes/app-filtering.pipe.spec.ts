import { AppFilteringPipe } from './app-filtering.pipe';

describe('AppFilteringPipe', () => {
  it('create an instance', () => {
    const pipe = new AppFilteringPipe();
    expect(pipe).toBeTruthy();
  });
});
